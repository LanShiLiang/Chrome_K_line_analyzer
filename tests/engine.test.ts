import{describe,expect,it}from'vitest';import{analyzeMarket}from'../src/core/analysis/engine';import{DEFAULT_CONFIG,type Candle,type MarketData}from'../src/core/model/types';
const market=(candles:Candle[]):MarketData=>({id:'m',siteId:'test',candles,source:{url:'https://test/kline',adapterId:'test',capturedAt:0},quality:{valid:true,candleCount:candles.length,missingFields:[],warnings:[],score:100}});
const base=Array.from({length:90},(_,i):Candle=>({timestamp:i*60000,open:100,high:102,low:98,close:100+(i%3),volume:100}));
describe('analyzeMarket',()=>{it('detects a volume-confirmed breakout',()=>{const candles=[...base,{timestamp:91*60000,open:102,high:110,low:101,close:109,volume:300}];const r=analyzeMarket(market(candles),DEFAULT_CONFIG);expect(r.stage).toBe('MARKUP');expect(r.signal.action).toBe('BUY');expect(r.signal.reasonCodes).toContain('B003')});it('degrades safely when no data exists',()=>{const r=analyzeMarket(market([]),DEFAULT_CONFIG);expect(r.signal.action).toBe('HOLD');expect(r.signal.confidence).toBe(0)})});
