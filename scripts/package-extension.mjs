import{mkdir,readFile,writeFile}from'node:fs/promises';import{createWriteStream}from'node:fs';import{resolve}from'node:path';import{createHash}from'node:crypto';import{execFileSync}from'node:child_process';
const pkg=JSON.parse(await readFile('package.json','utf8'));await mkdir('release',{recursive:true});const zip=resolve('release',`k-line-analyzer-${pkg.version}.zip`);if(process.platform==='win32')execFileSync('powershell',['-NoProfile','-Command',`Compress-Archive -Path 'dist\\*' -DestinationPath '${zip}' -Force`],{stdio:'inherit'});else execFileSync('zip',['-r',zip,'.'],{cwd:resolve('dist'),stdio:'inherit'});const bytes=await readFile(zip);await writeFile('release/checksums.txt',`${createHash('sha256').update(bytes).digest('hex')}  ${zip.split(/[\\/]/).at(-1)}\n`);createWriteStream('release/.keep').end();
